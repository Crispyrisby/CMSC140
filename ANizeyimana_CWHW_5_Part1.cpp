/*
#include <iostream>
using namespace std;

int main() {
    // First section (i) - Using a while loop
    cout << ("i")<<endl;
    int stars_i = 10;
    int i = 0;
    while (i < stars_i) {
        cout << "* ";
        i++;
    }
    cout << endl;

    // Second section (ii) - Using a do-while loop
    cout << ("ii")<<endl;
    int stars_ii = 20;
    int ii = 0;
    do {
        cout << "* ";
        ii++;
    } while (ii < stars_ii);
    cout << endl;

    // Rest of the sections - Nested for loops
    cout << ("iii") << endl;
    for (int row = 1; row <= 10; row++) {
        for (int col = 1; col <= row; col++) {
            cout << "* ";
        }
        cout << endl;
    }

    cout << ("iv") << endl;
    for (int row = 1; row <= 20; row++) {
        for (int col = 1; col <= row; col++) {
            cout << "* ";
        }
        cout << endl;
    }

    cout << ("v") << endl;
    for (int row = 10; row >= 1; row--) {
        for (int col = 1; col <= row; col++) {
            cout << "* ";
        }
        cout << endl;
    }


    cout << ("vi") << endl;
    for (int row = 20; row >= 1; row--) {
        for (int col = 1; col <= row; col++) {
            cout << "* ";
        }
        cout << endl;
    }

    return 0;
}

*/
